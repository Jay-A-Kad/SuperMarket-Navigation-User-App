<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.supermarket">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Supermarket"
        tools:replace="android:theme">
        <activity android:name=".cartFunction"></activity>
        <activity android:name=".electronicTab" />
        <activity android:name=".clothingTab" />
        <activity android:name=".UserProfile" />
        <activity android:name=".UserLogin" />
        <activity android:name=".UserSignUp" />
        <activity
            android:name=".grocceryTab"
            android:label="@string/title_activity_groccery_tab"
            android:theme="@style/Theme.Supermarket.NoActionBar" />
        <activity android:name=".Store" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>